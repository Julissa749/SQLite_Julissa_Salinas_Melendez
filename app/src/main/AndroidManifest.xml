<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.julissa_salinas702">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Julissa_Salinas702"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Lista_articulos_recyclerview"></activity>
        <activity
            android:name=".MainActivity"
            android:label="Julissa Salinas SQLite"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_detalle_articulos" />
        <activity android:name=".listview_articulos" />
        <activity android:name=".activity_consulta" />
    </application>

</manifest>